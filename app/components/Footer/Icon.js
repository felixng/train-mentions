import styled from 'styled-components';

const Icon = styled.i`
  margin-left: 0.25em;

  &:before {
    content: '';
    display: inline-block;
    height: 100%; 
    vertical-align: middle;
  }
`;

export default Icon;
